#include<stdio.h>

int evenoddcheck(int);

int main(void)
{
    int num;
    printf("input a number:");
    scanf("%d",&num);
    evenoddcheck(num);
}
int num;
printf("input a number: ");
scanf("%d",&num);

check(numb);
}

int evenoddcheck(int num)
{
if(num%2==0)
{
printf("The number is even: %d",num);
}
else
{
printf("The number is odd: %d",num);
}
}
